import java.util.HashMap;

public class Q409 {
    class Solution {
        public int longestPalindrome(String s) {
            if(s.length() == 0)
                return 0;
            int[] cnts = new int[52];
            for(char c: s.toCharArray())

        }
    }
}
